"""client — GUI delivery layer: Tk window, Pillow rendering, and (soon) the network client.

Depends only on the shared core engine package. Must not own: game rules,
simulation, or server transport concerns.
"""
